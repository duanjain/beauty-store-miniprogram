# 程序设计说明书

## 1. 引言

### 1.1 编写目的
本文档旨在详细描述“化妆品商城小程序”的系统架构、功能模块设计、数据库设计及接口设计，为软件开发、测试、维护及后续升级提供技术依据。本文档适用于开发人员、测试人员及项目管理人员。

### 1.2 项目背景
随着移动互联网的普及，线上购物与服务预约已成为用户的主流消费方式。本项目旨在开发一款集商品销售、服务预约、会员管理于一体的微信小程序，帮助商家实现线上线下业务融合，提升运营效率与用户体验。

### 1.3 术语定义
*   **小程序**：运行于微信客户端内的轻量级应用。
*   **云开发 (CloudBase)**：腾讯云提供的云原生一体化开发环境，包含云函数、云数据库、云存储等能力。
*   **云函数 (Cloud Function)**：运行在云端的代码，用于处理业务逻辑。
*   **OpenID**：微信用户在当前小程序中的唯一标识。

---

## 2. 系统总体设计

### 2.1 系统架构
本系统采用典型的 **C/S (Client/Server)** 架构，利用微信云开发提供的 Serverless 能力构建后端服务。

*   **客户端 (Client)**：
    *   基于微信小程序原生框架开发。
    *   使用 WXML 构建页面结构，WXSS 定义样式，JavaScript 处理交互逻辑。
    *   负责用户界面展示、用户输入响应及与云端的数据交互。

*   **服务端 (Server - CloudBase)**：
    *   **云函数**：承载核心业务逻辑（如订单创建、支付统一下单、库存扣减、定时任务等）。
    *   **云数据库 (NoSQL)**：存储用户信息、商品数据、订单记录、预约信息等结构化数据。
    *   **云存储**：存储商品图片、Banner 图等非结构化资源。

### 2.2 技术栈
*   **前端**：微信小程序原生开发 (WXML, WXSS, JS, JSON)
*   **后端**：Node.js (云函数环境)
*   **数据库**：MongoDB (云开发数据库)
*   **开发工具**：微信开发者工具, VS Code

---

## 3. 功能模块设计

### 3.1 模块划分
系统主要划分为以下五大模块：

1.  **用户模块**
    *   **登录注册**：基于微信 OpenID 实现静默登录与自动注册。
    *   **个人信息管理**：头像、昵称、手机号绑定。
    *   **收货地址管理**：增删改查收货地址。
    *   **会员中心**：积分查询、积分明细查看。

2.  **商品模块**
    *   **商品展示**：首页推荐、分类列表筛选。
    *   **商品详情**：展示图片、价格、销量、详细描述。
    *   **分类管理**：支持多级分类导航。

3.  **交易模块**
    *   **购物车**：添加商品、数量调整、选中结算。
    *   **订单创建**：确认收货地址、计算金额、生成订单。
    *   **支付流程**：调用微信支付接口完成付款。
    *   **订单管理**：待付款、待发货、待收货、已完成、退款/售后状态流转。

4.  **预约模块**
    *   **服务展示**：展示美容/护理服务项目及详情。
    *   **在线预约**：选择服务、日期、时间段（自动过滤不可用时间）。
    *   **预约管理**：查看我的预约、取消预约。

5.  **后台管理模块 (Admin)**
    *   **权限控制**：基于用户 Role 字段判断管理员身份。
    *   **商品管理**：发布新商品、编辑现有商品、上下架操作。
    *   **服务管理**：发布与编辑服务项目。
    *   **订单/预约处理**：查看订单详情、处理发货、核销预约。

### 3.2 核心流程设计

#### 3.2.1 下单流程
1.  用户将商品加入购物车或直接点击购买。
2.  进入“确认订单”页面，选择收货地址。
3.  前端调用 `createOrder` 云函数，传入商品列表。
4.  云函数校验库存与价格，创建 `pending` 状态订单，返回订单号。
5.  前端调用 `pay` 云函数获取支付参数。
6.  调用 `wx.requestPayment` 发起支付。
7.  支付成功后，微信回调云函数更新订单状态为 `paid`。

#### 3.2.2 预约流程
1.  用户进入服务详情页，点击“立即预约”。
2.  前端请求 `getUnavailableTimeSlots` 云函数，获取指定日期的已占用时间段。
3.  用户选择可用时间段并提交。
4.  云函数校验时间冲突，写入 `appointments` 集合，状态为 `pending`。
5.  若需预付定金，进入支付流程；否则直接预约成功。

---

## 4. 数据库设计

系统使用云开发数据库（文档型数据库），主要集合设计如下：

### 4.1 用户集合 (`users`)
| 字段名 | 类型 | 描述 |
| :--- | :--- | :--- |
| _id | String | 用户 OpenID (主键) |
| nickName | String | 昵称 |
| avatarUrl | String | 头像 URL |
| role | String | 角色 ('user' / 'admin') |
| points | Number | 会员积分 |
| createTime | Date | 注册时间 |

### 4.2 商品集合 (`products`)
| 字段名 | 类型 | 描述 |
| :--- | :--- | :--- |
| _id | String | 商品 ID |
| title | String | 商品标题 |
| price | Number | 价格 |
| originalPrice | Number | 原价 |
| coverImage | String | 封面图 |
| images | Array | 轮播图列表 |
| categoryId | String | 分类 ID |
| stock | Number | 库存 |
| isRecommend | Boolean | 是否推荐 |
| status | String | 状态 ('on' 上架 / 'off' 下架) |

### 4.3 订单集合 (`orders`)
| 字段名 | 类型 | 描述 |
| :--- | :--- | :--- |
| _id | String | 订单号 |
| _openid | String | 用户 OpenID |
| products | Array | 购买商品列表（快照） |
| totalFee | Number | 订单总金额 |
| address | Object | 收货地址快照 |
| status | String | 状态 (pending/paid/shipped/completed/refunded) |
| createTime | Date | 下单时间 |

### 4.4 预约集合 (`appointments`)
| 字段名 | 类型 | 描述 |
| :--- | :--- | :--- |
| _id | String | 预约 ID |
| serviceId | String | 服务 ID |
| serviceName | String | 服务名称 |
| date | String | 预约日期 (YYYY-MM-DD) |
| time | String | 预约时间段 (HH:mm) |
| status | String | 状态 (pending/confirmed/cancelled/completed) |
| contactName | String | 联系人姓名 |
| contactPhone | String | 联系电话 |

---

## 5. 接口设计 (云函数)

系统不使用传统的 RESTful API，而是通过 `wx.cloud.callFunction` 调用云函数。

### 5.1 支付接口 (`pay`)
*   **功能**：处理微信支付统一下单。
*   **输入**：
    *   `type`: 'unifiedOrder'
    *   `orderId`: 订单号
    *   `totalFee`: 金额（分）
*   **输出**：微信支付签名参数包。

### 5.2 获取不可用时间段 (`getUnavailableTimeSlots`)
*   **功能**：查询指定服务在指定日期的已被预约时间段。
*   **输入**：
    *   `serviceId`: 服务 ID
    *   `date`: 日期
*   **输出**：`timeSlots`: Array<String> (已占用时间列表)

### 5.3 订单状态更新 (`orderUpdate`)
*   **功能**：管理员更新订单状态（如发货）。
*   **输入**：
    *   `orderId`: 订单号
    *   `action`: 操作类型 ('ship', 'complete', 'refund')
*   **输出**：操作结果状态。

---

## 6. 安全设计

1.  **数据权限**：利用云开发数据库权限设置（Security Rules），限制用户只能读写自己的数据（如订单、地址），管理员拥有全库读写权限。
2.  **输入校验**：云函数端对前端传入的参数（如金额、库存）进行二次校验，防止恶意篡改。
3.  **支付安全**：支付金额在云端根据数据库商品价格重新计算，不依赖前端传入的金额。
4.  **内容安全**：集成微信内容安全接口，对用户提交的文本和图片进行违规检测。
